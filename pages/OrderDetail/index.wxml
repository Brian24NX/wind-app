<!--pages/OrderDetail/index.wxml-->
<view class="moreDetailContainer">
  <one detail="{{detail}}" showSearch="{{showSearch}}" wx:if="{{!loading}}" languageContent="{{languageContent}}"></one>
</view>
